import { VegetablePlant } from "../src/FruitTree";
const chai = require("chai");
const assert = chai.assert;

// Add a describe block called `"Vegetable Plant Tests"` and pass in an empty function.